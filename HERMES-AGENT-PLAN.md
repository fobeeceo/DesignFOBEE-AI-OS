# HERMES-AGENT-PLAN — 자율 에이전트 도입 계획서 v1.0

> 작성 2026-08-11 · 상태 **CEO 승인 대기** · 상위 규범 [CEO-CHARTER.md](CEO-CHARTER.md) §16-B(승인규칙 6항목) · [CLAUDE.md](CLAUDE.md) §0
>
> 대표님이 영상에서 보신 "헤르메스"는 **Nous Research의 오픈소스 자율 에이전트 Hermes Agent**다.
> 사내에 이미 있는 `lib/courier/**`(전령)와는 이름만 비슷한 다른 것이라, 이 문서에서 관계를 정리한다.

---

## 0. 확인하지 못한 것 (먼저 밝힌다)

CLAUDE.md §0-2 원칙 7에 따라, 이 문서에서 **직접 확인하지 못한 것**을 먼저 적는다.

- **공식 문서를 직접 열지 못했다.** `hermes-agent.nousresearch.com`·`blogs.nvidia.com`이 이 작업 환경의 네트워크 정책에 막혀 있다. 아래 §1 사양은 **웹 검색 결과 요약 기준**이며, 설치 착수 전에 공식 저장소에서 재확인해야 한다.
- **실제 설치·구동을 해보지 않았다.** 이 세션은 임시 컨테이너라 상주 프로그램을 검증할 수 없다(§3).
- **비용은 실측이 아니라 추정이다.** §4의 금액은 일반적인 API 단가 기준의 개략치이며, 실제 청구액은 사용량에 따라 달라진다.

---

## 1. Hermes Agent란 무엇인가

| 항목 | 내용 |
|---|---|
| 만든 곳 | Nous Research |
| 라이선스 | MIT (오픈소스) |
| 공식 저장소 | `github.com/NousResearch/hermes-agent` — **이것 하나만 공식** |
| 성격 | 내 서버·PC에 **상주**하는 자율 에이전트 |
| 핵심 기능 | 영구 기억 · 스스로 스킬 생성(한 번 한 일을 절차 문서로 남겨 재사용) · 서브에이전트 · MCP 도구 연결 |
| 할 수 있는 것 | 웹 검색, 브라우저 자동화, 코드 실행, 파일 편집, 외부 서비스 연동 |
| 실행 요건 | Linux · macOS · WSL2 / Git / Python 3.11+ / VPS 1 vCPU · 1 GB (로컬 도구·메모리 사용 시 2 GB 이상) |
| 배포 | Docker 이미지 `nousresearch/hermes-agent:latest`, 프로필당 컨테이너 1개, `gateway run` |
| 모델 | OpenAI 호환 API면 무엇이든(64k 컨텍스트 필요) — OpenRouter · Anthropic · OpenAI · 로컬 Ollama |
| 접속 경로 | Telegram · Discord · Slack · WhatsApp · Signal · Email · CLI |

### ⚠️ 가짜 사이트 주의

검색 시 `hermes-agent.org`, `hermesagents.net`, `hermesagent.agency`, `hermes-agent.ai`, `hermesagent.*` 등 **공식을 흉내 낸 도메인이 다수** 확인됐다.
Hermes Agent는 **파일 편집·코드 실행 권한을 갖는 프로그램**이라, 가짜 배포본을 설치하면 서버 전체가 넘어간다.
**설치 스크립트·Docker 이미지는 `github.com/NousResearch/hermes-agent`에서 확인된 경로로만 받는다.** 검색 결과 상단 링크를 그대로 클릭하지 않는다.

---

## 2. 우리 시스템과 어떻게 나눌 것인가 — 손과 머리

대표님 결정(2026-08-11): **둘 다 쓰되 역할을 나눈다.**

```
   [Hermes Agent]  ← 손 · 기억           [전령 Courier]  ← 사내 데이터 창구
   상주 · 실행권한 · 학습                 lib/courier/** · /api/hq/courier
   Telegram/Slack에서 대표와 대화          규칙 기반 · LLM 없음 · 발송 안 함
            │                                        ▲
            └──────── HTTP 호출 ──────────────────────┘
              GET /api/hq/courier/morning?format=md
```

| | Hermes Agent | 전령(Courier) |
|---|---|---|
| 사는 곳 | 대표님 PC 또는 VPS(상주) | Vercel 배포 웹앱 안 |
| 기억 | 영구 기억 + 스킬 축적 | 없음(매번 계산) |
| 실행 권한 | 있음 | **없음**(봉투 생성까지) |
| 비용 | LLM API 종량 과금 | 0원 |
| 아는 것 | 범용 | **사내 SSOT**(ERP·가맹 공개기준·AI 조직도) |

**이렇게 나누는 이유**: 자율 에이전트에게 사내 규칙(무엇을 공개하고 무엇을 상담에서만 말하는가, §0-5)을 매번 프롬프트로 알려주면 언젠가 빠뜨린다. 규칙은 코드(전령)에 두고, 에이전트는 그 결과를 **받아서 옮기게** 한다. 그러면 에이전트가 바뀌어도 규칙은 그대로다.

### 첫 연동 지점 — 아침 브리핑 (구현 완료)

```
매일 07:00  Hermes Agent
   → GET https://<도메인>/api/hq/courier/morning?format=md
      Authorization: Bearer <N8N_SERVICE_TOKEN>
   → 받은 마크다운을 그대로 Telegram으로 대표님께 전송
```

전령이 **문안을 완성해서** 주므로 에이전트는 요약·각색하지 않고 그대로 옮긴다. 중간에 다시 쓰게 하면 그 과정에서 문구가 바뀔 수 있다.

---

## 3. 왜 이 세션에서 설치할 수 없는가

지금 이 작업 환경은 **임시 컨테이너**다. 세션이 끝나면 반납되므로, 여기 설치한 상주 프로그램은 사라진다.
Hermes Agent는 24시간 떠 있어야 의미가 있으므로 **대표님 PC 또는 VPS**에서 실행해야 한다.

→ 설치·인증은 **대표님이 직접 하셔야 하는 작업(Owner Action Required)**이다. 아래 §5에 단계와 예상 소요를 분리해 적었다.

---

## 4. CEO 승인이 필요한 항목 (CEO-CHARTER §16-B)

6항목 중 **2개**에 해당한다.

| 승인 항목 | 내용 | 추정 비용 |
|---|---|---|
| **비용 발생** | LLM API 종량 과금. 아침 브리핑 1회 전송 정도면 미미하나, 자율 작업을 맡길수록 늘어난다. | 월 3만~15만원 추정(사용량 의존, **실측 아님**) |
| **외부 서비스 가입** | OpenRouter 등 API 제공사 계정. VPS를 쓸 경우 호스팅 계정도 해당. | VPS 월 5천~1만원(1 vCPU·1 GB 기준 추정) |

**비용을 0원으로 시작하는 선택지도 있다**: 대표님 PC에 설치하고 로컬 Ollama를 붙이면 API 비용이 없다. 다만 PC를 켜둬야 하고 응답 품질이 낮아진다. 먼저 이 방식으로 감을 잡은 뒤 유료 API로 옮기는 것을 권한다.

---

## 5. 도입 단계 (승인 시)

각 단계의 **대표님 작업**과 **AI 작업**을 분리했다.

| 단계 | 내용 | 담당 | 예상 소요 |
|---|---|---|---|
| 1 | 공식 저장소에서 사양·설치 절차 재확인(§0 미확인 항목 해소) | AI | 10분 |
| 2 | 설치 위치 결정 — 대표님 PC vs VPS | **대표** | 5분 |
| 3 | 모델 결정 — 로컬 Ollama(무료) vs OpenRouter 등(유료) | **대표** | 5분 |
| 4 | Docker 설치 + `nousresearch/hermes-agent` 기동 | **대표**(AI가 명령어 제공) | 30분 |
| 5 | Telegram 연결(봇 생성·토큰 입력) — TODO.md에 이미 대기 중인 항목과 동일 | **대표** | 15분 |
| 6 | 전령 API 연동: `N8N_SERVICE_TOKEN` 발급 → 에이전트에 등록 | **대표**(AI가 절차 제공) | 10분 |
| 7 | 아침 브리핑 스킬 등록(매일 07:00 호출→전송) | AI가 문안 작성, 대표가 입력 | 15분 |
| 8 | 1주일 시범 운영 후 확대 여부 판단 | 공동 | — |

**단계 1~3까지는 승인 없이 가능하다**(조사·결정 준비). 4단계부터가 승인 대상이다.

---

## 6. 도입하지 않을 경우

전령(Courier)만으로도 아침 브리핑은 동작한다. 다만 **누군가 URL을 호출해줘야** 한다.
지금은 그 자리가 비어 있어서, 브리핑이 만들어져도 대표님께 자동으로 가지 않는다(현재 채널 4개가 전부 미연동, `/api/hq/courier` GET으로 확인 가능).

대안은 **n8n 예약 실행**이다. TODO.md에 이미 "n8n 아침 브리핑 활성화"가 대기 중이며, 이쪽은 추가 비용이 없다. 다만 n8n은 정해진 것만 하고 배우지 않는다.

| | Hermes Agent | n8n 예약 실행 |
|---|---|---|
| 비용 | LLM 종량 | 0원 |
| 기억·학습 | 있음 | 없음 |
| 시킨 것 외 | 스스로 판단 | 못 함 |
| 설치 부담 | 중간 | 낮음(이미 구축됨, 인증만 남음) |

**권고**: n8n으로 아침 브리핑을 먼저 띄워 **자동 전달이 실제로 되는지** 확인하고, 그다음 Hermes Agent를 얹는다. 순서를 바꾸면 브리핑이 안 갔을 때 원인이 전령인지 에이전트인지 구분하기 어렵다.

---

## 7. 이름 정리 (완료)

같은 이름 두 개를 피하기 위해 사내 모듈 이름을 바꿨다(2026-08-11).

| 이전 | 이후 |
|---|---|
| 헤르메스(전령) | **전령(Courier)** |
| `lib/hermes/**` | `lib/courier/**` |
| `agents/hermesAgent.ts` | `agents/courierAgent.ts` |
| `/api/hq/hermes` | `/api/hq/courier` |

"헤르메스"는 앞으로 **Nous Research의 Hermes Agent만** 가리킨다.
